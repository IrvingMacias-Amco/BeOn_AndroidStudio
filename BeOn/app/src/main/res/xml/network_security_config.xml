<?xml version="1.0" encoding="utf-8"?>
<!--
  Política de red de BeOn.

  - Producción (Amplify, Mux, Sanity CDN, etc.): solo HTTPS.
  - Desarrollo: se permite HTTP en blanco SOLO para los hosts del BFF local:
      * 10.0.2.2   → loopback del host visto desde el emulador Android.
      * localhost  → cuando se corre vía adb reverse o sobre el propio dispositivo.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
