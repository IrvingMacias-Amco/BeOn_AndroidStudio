# Copy to .env and fill with rotated credentials.
# Do NOT commit .env — it is gitignored.

# Sanity CMS
SANITY_PROJECT_ID=
SANITY_DATASET=production
SANITY_API_TOKEN=

# Mux (backend only — never ship to Android)
MUX_TOKEN_ID=
MUX_TOKEN_SECRET=
# MUX_ENV_KEY=
